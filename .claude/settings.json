{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; base=$(basename -- \"$f\"); if [ \"$base\" = \"private.tex\" ]; then echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"ask\",\"permissionDecisionReason\":\"private.tex holds a real phone number - confirm this edit is intentional.\"}}'; else echo '{}'; fi; }"
          }
        ]
      }
    ]
  }
}
